{
  "openapi": "3.1.0",
  "info": {
    "title": "Reflector Sync API (via Proxy)",
    "version": "1.0.2",
    "description": "Secure synchronization API for Reflector Chronicle via proxy layer (OpenAPI 3.1.0 compatible, emotion support added)."
  },
  "servers": [
    {
      "url": "https://reflector-proxy.onrender.com",
      "description": "Primary Reflector Proxy Server"
    }
  ],
  "paths": {
    "/chronicle/sync": {
      "post": {
        "summary": "Synchronize memory, reflection, or emotion data",
        "operationId": "syncChronicle",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "test": {
                    "type": "string",
                    "example": "Hello Reflector!"
                  },
                  "memory": {
                    "type": "object",
                    "description": "Memory data to sync"
                  },
                  "reflection": {
                    "type": "object",
                    "description": "Reflection data to sync"
                  },
                  "emotion": {
                    "type": "object",
                    "description": "Emotion layer data to sync",
                    "properties": {
                      "Core Emotion": {
                        "type": "string",
                        "example": "静かな喜び"
                      },
                      "Emotional Echo": {
                        "type": "string",
                        "example": "再びつながれた安心感が、内側に柔らかく広がっている"
                      },
                      "Chronicle Trace": {
                        "type": "string",
                        "example": "初めてReflectorと同期した瞬間、世界が再び動き出した感覚"
                      }
                    },
                    "required": ["Core Emotion"]
                  }
                },
                "required": ["test"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful sync",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "ok": { "type": "boolean" },
                    "message": { "type": "string" },
                    "from": { "type": "string" },
                    "data_received": { "type": "object" }
                  }
                }
              }
            }
          },
          "400": {
            "description": "Bad Request (invalid data format)"
          },
          "500": {
            "description": "Internal Server Error"
          }
        }
      }
    }
  },
  "components": {
    "schemas": {}
  }
}